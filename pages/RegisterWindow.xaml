<Window x:Class="BankSystem.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Регистрация нового клиента" 
                   FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Логин:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="LoginTextBox" Width="200"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Пароль:" Width="100" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBox" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Номер телефона:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="PhoneNumberBox" Width="200" MaxLength="15"/>
        </StackPanel>

        <Button Grid.Row="4" Content="Зарегистрироваться" 
                Width="200" Height="35" 
                HorizontalAlignment="Center" 
                Click="Register_Click"/>
    </Grid>
</Window>
